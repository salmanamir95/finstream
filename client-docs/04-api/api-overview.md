# API overview

The FinStream API allows external applications to interact with the FinStream account-management capability. The local base URL is `http://localhost:8080`; production URL and authentication are not yet provided. JSON is used for bodies and responses. See [API reference](api-reference.md) for actual paths.
