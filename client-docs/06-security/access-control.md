# Access control

Current access control is not implemented. Before production, access should be granted by least privilege, verify the caller can act on the referenced customer/account, separate administrative rate/overdraft/status powers, and record privileged changes. Clients should never embed long-lived credentials in source code.
