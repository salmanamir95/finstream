# ADR-003: Common as a reusable Java library

**Status:** Proposed correction. **Decision:** plain JAR, no application bootstrap. **Alternative:** Spring Boot application. **Consequence:** safe reuse/minimal runtime coupling; optional auto-configuration belongs in a separate starter.
