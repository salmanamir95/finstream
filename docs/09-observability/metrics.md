# Metrics

Proposed metrics: HTTP request count/latency/error rate by route/status; JVM and connection-pool health; database latency; account creation/update counts; validation/conflict failures; event publish/consumer lag when events exist. Alert on sustained error budget burn, unavailable health checks, exhausted pools, and failed migrations—not every individual error.
